{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install motor==3.6.0 pymongo==4.9.2 --force-reinstall -q && cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"
  }
}

